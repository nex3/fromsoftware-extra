## 0.1.1

* Add a `fromsoftware-extra-shared` crate and move `OnLoadType` and
  `SaveLoadHook` into it from `darksouls3-extra`.

* Add a `sekiro-extra` crate with an `on_save_load()` function.

## 0.1.0

* Initial release.
